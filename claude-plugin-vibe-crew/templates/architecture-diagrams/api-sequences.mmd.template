%% API Interaction Sequences
%% Generated by VibeCrew during Tier 1 foundation
%% Add primary CRUD flows and third-party integration sequences
%% Update during Tier 2 when routes, API endpoints, or middleware change

sequenceDiagram
    participant C as Client
    participant S as Server
    participant A as Auth Provider
    participant D as Database

    Note over C,D: Authentication Flow
    C->>S: POST /api/auth/login
    S->>A: Verify credentials
    A-->>S: Token / session
    S-->>C: Set cookie / return token

    Note over C,D: Primary CRUD Flow (placeholder)
    C->>S: GET /api/resource
    S->>D: SELECT query
    D-->>S: Result set
    S-->>C: JSON response

    C->>S: POST /api/resource
    S->>D: INSERT query
    D-->>S: Created record
    S-->>C: 201 Created

    %% Add additional sequences below based on VISION.md features
    %% Example SSR flow:
    %% Note over C,S: Server-Side Rendering
    %% C->>S: GET /page
    %% S->>D: Fetch page data
    %% D-->>S: Data
    %% S-->>C: Rendered HTML
