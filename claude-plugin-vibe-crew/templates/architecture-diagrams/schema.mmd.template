%% Database Schema Diagram
%% Generated by VibeCrew during Tier 1 foundation
%% Add domain entities from VISION.md user personas and core features
%% Update during Tier 2 when migrations or models change

erDiagram
    User {
        string id PK
        string email UK
        string name
        string role
        datetime created_at
        datetime updated_at
    }

    %% Add domain entities below based on VISION.md
    %% Example:
    %% User ||--o{ Post : creates
    %% Post {
    %%     string id PK
    %%     string title
    %%     text content
    %%     string user_id FK
    %%     datetime created_at
    %% }
