export interface DatavizPattern {
  name: string;
  category: 'chart-types' | 'composition' | 'interaction' | 'accessibility' | 'real-time' | 'responsive';
  description: string;
  implementation: string;
  a11y: string;
  docsUrl: string | null;
}

export const categoryLabels: Record<DatavizPattern['category'], string> = {
  'chart-types': 'Chart Types',
  composition: 'Dashboard Composition',
  interaction: 'Chart Interaction',
  accessibility: 'Accessibility',
  'real-time': 'Real-Time & Streaming',
  responsive: 'Responsive & Mobile',
};

export const categoryOrder: DatavizPattern['category'][] = [
  'chart-types', 'composition', 'interaction', 'accessibility', 'real-time', 'responsive',
];

export const datavizPatterns: DatavizPattern[] = [
  // --- Chart Types ---
  {
    name: 'Line Chart',
    category: 'chart-types',
    description: 'Plots data points connected by lines to show trends over continuous intervals (time series). The default choice for showing change over time. Use when the x-axis is ordered (dates, sequential steps).',
    implementation: 'shadcn: Chart (Recharts wrapper). Use LineChart with Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend. Tremor: LineChart component. For multiple series, use distinct stroke colors from design tokens.',
    a11y: 'Provide a data table alternative via a toggle or <details> element. Use aria-label on the chart container describing the trend. Never rely on color alone — add point markers or dashed/solid line styles per series.',
    docsUrl: 'https://ui.shadcn.com/docs/components/chart',
  },
  {
    name: 'Bar Chart',
    category: 'chart-types',
    description: 'Uses rectangular bars to compare discrete categories. Research shows bar charts are the easiest chart type for users to interpret accurately (NN/G). Vertical for categories, horizontal when labels are long.',
    implementation: 'shadcn: Chart with BarChart, Bar, XAxis, YAxis. Tremor: BarChart. Use horizontal layout when category labels exceed 8 characters. Add value labels on bars for small datasets (<10 bars).',
    a11y: 'Each bar should be focusable with aria-label including category and value. Provide screen-reader-accessible data table. Minimum bar color contrast of 3:1 against background.',
    docsUrl: 'https://ui.shadcn.com/docs/components/chart',
  },
  {
    name: 'Area Chart',
    category: 'chart-types',
    description: 'A line chart with the area below filled, emphasizing volume or cumulative totals. Stacked areas show part-to-whole over time. Use when the magnitude matters, not just the trend.',
    implementation: 'shadcn: Chart with AreaChart, Area (stackId for stacking). Tremor: AreaChart with stack prop. Use gradient fills (opacity 0.3 → 0) for depth. Limit to 4 stacked series for readability.',
    a11y: 'Stacked areas are hard to read for colorblind users — provide a toggle to un-stack. Include data table alternative. Label each area with a visible legend.',
    docsUrl: null,
  },
  {
    name: 'Pie & Donut Chart',
    category: 'chart-types',
    description: 'Shows part-to-whole relationships as angular slices. Donut variant adds a center metric. Limit to 5-6 slices maximum — research shows users struggle to compare similar angles (NN/G). Always sort by size.',
    implementation: 'shadcn: Chart with PieChart, Pie, Cell. Tremor: DonutChart. Place the primary KPI in donut center. Group small slices (<5%) into "Other". Use direct labels not just legend.',
    a11y: 'Provide percentage values as direct labels or in tooltip. Include a data table alternative — pie charts are one of the hardest chart types for screen readers. Use patterns (stripes, dots) in addition to color.',
    docsUrl: null,
  },
  {
    name: 'Scatter Plot',
    category: 'chart-types',
    description: 'Plots individual data points on two axes to reveal correlations, clusters, and outliers. Use when exploring relationships between two quantitative variables. NN/G notes position is a preattentive variable — outliers are spotted instantly.',
    implementation: 'Recharts: ScatterChart with Scatter, XAxis, YAxis, ZAxis (for bubble size). Tremor: Not built-in, use Recharts directly. Add reference lines for averages. Use color for a third dimension.',
    a11y: 'Announce data count and visible pattern ("120 points, positive correlation"). Each point focusable with tooltip showing all dimensions. Provide tabular alternative for precise values.',
    docsUrl: null,
  },
  {
    name: 'Heatmap',
    category: 'chart-types',
    description: 'Uses color intensity on a grid to show magnitude across two categorical dimensions. Common for time-of-day activity, correlation matrices, and geographic density. The eye is drawn to hotspots instantly.',
    implementation: 'Build with a CSS Grid of colored cells or D3.js for complex heatmaps. Use a sequential color scale (e.g., white → blue). Show value on hover. Add a color legend with min/max labels.',
    a11y: 'Never rely on color alone — show values on hover and in a data table. Use aria-label per cell: "Monday 9am: 42 requests". Provide a high-contrast mode with pattern fills.',
    docsUrl: null,
  },
  {
    name: 'Sparkline',
    category: 'chart-types',
    description: 'A tiny, word-sized chart (typically a line or bar) embedded inline with text or inside a table cell or KPI card. Invented by Edward Tufte to show trends without axis labels. Shows direction, not precise values.',
    implementation: 'Recharts: LineChart with no axes, grid, or tooltip. Set height to 24-32px. Tremor: SparkChart. Color the line or final point green/red to indicate positive/negative trend.',
    a11y: 'Add aria-label describing the trend: "Revenue: up 12% over 7 days". Sparklines are decorative context — ensure the actual value is available as text nearby.',
    docsUrl: null,
  },
  {
    name: 'Gauge / Radial Chart',
    category: 'chart-types',
    description: 'A semicircular or circular meter showing a single value against a scale. Best for KPIs with known min/max (CPU usage, goal progress, health scores). Limit to one per card — multiple gauges waste space.',
    implementation: 'Recharts: RadialBarChart or custom SVG arc. Tremor: Not built-in, use custom. Show the numeric value prominently in the center. Color zones: green (good), yellow (warning), red (critical).',
    a11y: 'Display the numeric value as visible text — the visual arc is supplementary. aria-label="CPU usage: 73%". For color zones, add text labels ("Good", "Warning", "Critical") not just color.',
    docsUrl: null,
  },
  {
    name: 'Funnel Chart',
    category: 'chart-types',
    description: 'Shows progressive reduction through sequential stages (conversion funnels, sales pipelines). Each stage is a trapezoid narrower than the previous. Shows both absolute counts and conversion rates between steps.',
    implementation: 'Recharts: FunnelChart with Funnel, LabelList. Show both the count and percentage drop-off at each stage. Color from dark to light as values decrease. Add connector labels for stage-to-stage conversion.',
    a11y: 'Label each stage with name, count, and conversion rate. Provide a table alternative showing all stages. aria-label on container: "Conversion funnel: 1000 visitors to 42 purchases".',
    docsUrl: null,
  },
  {
    name: 'Treemap',
    category: 'chart-types',
    description: 'Nested rectangles sized by a quantitative value, showing hierarchical part-to-whole. Good for file system usage, budget allocation, or portfolio composition. Area encoding is less precise than bar length but shows hierarchy well.',
    implementation: 'Recharts: Treemap with customized content renderer. D3.js for complex treemaps with drill-down. Label rectangles directly — legends fail with many categories. Use color for a secondary dimension.',
    a11y: 'Each rectangle needs aria-label with category and value. Provide breadcrumb navigation for drill-down. Include a flat table view as alternative. Minimum rectangle size for touch targets: 44x44px.',
    docsUrl: null,
  },
  {
    name: 'Sankey Diagram',
    category: 'chart-types',
    description: 'Flow diagram where link width is proportional to flow quantity. Shows how values distribute from source to destination (user journeys, budget flows, energy transfers). Reveals leakage and dominant paths.',
    implementation: 'D3-sankey or Recharts Sankey. Nodes on left/right columns, links as curved paths. Color links by source or destination. Show flow quantity on hover. Limit to 15-20 nodes for readability.',
    a11y: 'Provide a flow table: Source → Destination → Quantity. Each link focusable with tooltip. aria-label on container summarizing the flow: "User journey: 60% of visitors reach checkout".',
    docsUrl: null,
  },
  {
    name: 'KPI Card',
    category: 'chart-types',
    description: 'A single large number with label, trend indicator, and optional sparkline. The most common dashboard element. Shows current value, comparison to previous period, and directional arrow. Place the 3-5 most critical KPIs at the top of any dashboard.',
    implementation: 'shadcn: Card with large text for value, Badge for trend. Tremor: Metric component. Include: label, value, delta (absolute or %), trend direction arrow, and optional sparkline.',
    a11y: 'Structure as a heading + value. Trend direction conveyed via text ("up 12%") not just arrow color. Use aria-live="polite" if the value updates in real-time.',
    docsUrl: null,
  },

  // --- Dashboard Composition ---
  {
    name: 'Dashboard Grid Layout',
    category: 'composition',
    description: 'Arrange charts and KPIs in a responsive grid with consistent card sizing. Place the most important metrics in the top-left (F-pattern scanning). Limit to 5-9 visible widgets to prevent cognitive overload (NN/G).',
    implementation: 'CSS Grid with named areas or auto-fill. Use 12-column grid for flexibility. shadcn: Card for each widget. Full-width charts on top, smaller cards in 2-3 column rows below. Gap: 1rem.',
    a11y: 'Use landmark regions: <main> for the dashboard, headings for each section. Logical tab order should match visual layout (left-to-right, top-to-bottom). Skip links to major sections.',
    docsUrl: null,
  },
  {
    name: 'Dashboard Header with Global Filters',
    category: 'composition',
    description: 'A sticky bar above all dashboard widgets containing date range picker, comparison toggle, and global filters. All charts respond to these shared controls. Prevents per-chart filter confusion.',
    implementation: 'shadcn: DatePickerWithRange + Select/Combobox in a sticky flex bar. Store filter state in URL params. All chart queries include the global date range. Show "Last updated: X" timestamp.',
    a11y: 'Landmark: <nav aria-label="Dashboard filters">. Date picker accessible via keyboard. Announce filter changes with aria-live. Indicate which charts are affected.',
    docsUrl: null,
  },
  {
    name: 'Chart Card with Header',
    category: 'composition',
    description: 'Each chart wrapped in a card with: title, subtitle/description, optional controls (time range tabs, export), and the chart below. Consistent card structure across all dashboard widgets.',
    implementation: 'shadcn: Card > CardHeader (title + description + action buttons) > CardContent (chart). Tremor: Card with Title. Add DropdownMenu for per-chart actions (export, expand, refresh).',
    a11y: 'Card title in <h3> or appropriate heading level. Chart container has role="img" with aria-label summarizing the data. Action buttons have descriptive labels.',
    docsUrl: null,
  },
  {
    name: 'Tabbed Dashboard Views',
    category: 'composition',
    description: 'Organize dashboard sections into tabs (Overview, Revenue, Users, Performance) to manage complexity without scrolling. The Overview tab shows the top 5-7 KPIs; detail tabs show full charts.',
    implementation: 'shadcn: Tabs with TabsList, TabsTrigger, TabsContent. Lazy-load chart data per tab to reduce initial page load. Persist active tab in URL params.',
    a11y: 'Standard tab pattern: role="tablist", role="tab" with aria-selected, role="tabpanel" with aria-labelledby. Arrow keys move between tabs. Tab panel announced on switch.',
    docsUrl: 'https://www.w3.org/WAI/ARIA/apg/patterns/tabs/',
  },
  {
    name: 'Comparison Layout',
    category: 'composition',
    description: 'Side-by-side or overlay chart layout for comparing two periods, segments, or scenarios. "This period vs last period" is the most common comparison. Overlay on same axes when scales match; split when they differ.',
    implementation: 'Overlay: two Line/Bar series on one chart with a legend. Side-by-side: two Chart components in a 2-column grid. Use solid for current, dashed for comparison. Tremor: comparison prop on charts.',
    a11y: 'Legend must clearly distinguish series. aria-label: "Revenue: current month vs previous month". Ensure line styles differ (solid vs dashed) — not just color.',
    docsUrl: null,
  },
  {
    name: 'Empty Dashboard State',
    category: 'composition',
    description: 'What to show when a dashboard has no data yet — because the user is new, the date range has no activity, or the data source is not connected. Never show blank chart containers.',
    implementation: 'Show illustration + message: "No data for this period" or "Connect your data source to get started". For new users, show sample data with a "This is sample data" banner. Include a CTA.',
    a11y: 'Empty state message must be in the chart region so screen readers encounter it. Use role="status" or aria-live for dynamic empty states when filters produce no data.',
    docsUrl: null,
  },

  // --- Chart Interaction ---
  {
    name: 'Tooltip on Hover',
    category: 'interaction',
    description: 'A floating card showing precise values when hovering over a data point or bar. The primary way users read exact values from charts. Show all series values at the hovered x-position for line/area charts.',
    implementation: 'Recharts: Tooltip with custom content renderer. Show formatted values with units. For line charts, use a shared tooltip (one per x-position, all series). Tremor: built-in tooltip.',
    a11y: 'Tooltip must appear on keyboard focus, not just mouse hover. Use aria-describedby to link data points to tooltip content. Tooltip should not obscure other data.',
    docsUrl: null,
  },
  {
    name: 'Crosshair',
    category: 'interaction',
    description: 'A vertical (or cross-shaped) reference line following the cursor across the chart, snapping to the nearest data point. Helps users track position and compare values across multiple line series.',
    implementation: 'Recharts: set cursor prop on Tooltip. For custom: overlay an SVG line at mouse x-position, snap to nearest data point. Show a dot on each line at the crosshair intersection.',
    a11y: 'Crosshair is a visual enhancement — not required for accessibility. Ensure the underlying tooltip provides the same value information via keyboard.',
    docsUrl: null,
  },
  {
    name: 'Brush / Range Selection',
    category: 'interaction',
    description: 'A draggable range selector below the main chart for zooming into a time window. Shows a miniature overview of the full dataset with handles to select a sub-range. Essential for large time series.',
    implementation: 'Recharts: Brush component below the chart. Show a mini area chart in the brush for context. Sync brush range with the main chart domain. Add reset button to restore full range.',
    a11y: 'Brush handles must be keyboard-operable (arrow keys to move, Shift+Arrow to resize). Announce the selected range: "Showing Jan 15 to Feb 28". Provide date inputs as alternative.',
    docsUrl: null,
  },
  {
    name: 'Drill-Down',
    category: 'interaction',
    description: 'Clicking a chart element navigates to a more detailed view. Click a bar to see its breakdown, click a time period to zoom in. Progressive disclosure for complex data — overview first, details on demand (Shneiderman\'s mantra).',
    implementation: 'Add onClick to chart elements. Navigate to a detail view or expand the chart in-place. Show breadcrumb for navigation back. Animate the transition for context preservation.',
    a11y: 'Clickable elements need role="button" or be actual <button> elements. Announce drill-down: "Showing breakdown for Q3 Revenue". Breadcrumb provides keyboard-accessible back navigation.',
    docsUrl: null,
  },
  {
    name: 'Chart Legend',
    category: 'interaction',
    description: 'A labeled key mapping colors/patterns to data series. Place below or to the right of the chart. Interactive legends that toggle series visibility are highly valued by users for multi-series charts.',
    implementation: 'Recharts: Legend with onClick to toggle series visibility. Show hidden series as grayed-out in legend. Tremor: built-in interactive legend. Place below chart on mobile, right on desktop.',
    a11y: 'Legend items must be keyboard-focusable with toggle via Enter/Space. Announce: "Revenue series hidden" / "Revenue series shown". Use patterns + color, not color alone.',
    docsUrl: null,
  },
  {
    name: 'Chart Annotations',
    category: 'interaction',
    description: 'Labels, arrows, or highlighted regions on a chart marking notable events (product launches, outages, promotions). Give context to data anomalies that users would otherwise investigate manually.',
    implementation: 'Recharts: ReferenceLine, ReferenceArea, ReferenceDot with custom labels. Position annotations at the relevant x/y coordinates. Use a distinct annotation color. Show detail on hover.',
    a11y: 'Each annotation must have a text label accessible to screen readers. Use aria-label on annotation markers. Include annotations in the data table alternative.',
    docsUrl: null,
  },
  {
    name: 'Chart Export',
    category: 'interaction',
    description: 'Allow users to download chart data as CSV/Excel or the chart image as PNG/SVG. Power users need raw data for their own analysis. Place export button in the chart card header.',
    implementation: 'CSV: serialize chart data array and trigger download. PNG: use html2canvas or svg-to-image on the chart container. Add DropdownMenu with "Download CSV", "Download PNG" options.',
    a11y: 'Export button needs aria-label="Export chart data". Download should trigger a file save dialog. Announce "Download started" via aria-live for screen reader users.',
    docsUrl: null,
  },

  // --- Accessibility ---
  {
    name: 'Data Table Alternative',
    category: 'accessibility',
    description: 'Every chart must have an equivalent data table that screen reader users can navigate. This is the single most important accessibility requirement for data visualization. Toggle between chart and table view.',
    implementation: 'Add a "View as table" button/toggle above each chart. Render the same data as a <table> with proper <thead>, <th>, <td>. Use shadcn DataTable for sortable tables. Hide/show with display toggle.',
    a11y: 'Table must include <caption> describing the data. Use scope="col" and scope="row" on headers. Table must contain all the information the chart conveys, including trends noted in annotations.',
    docsUrl: 'https://www.w3.org/WAI/tutorials/images/complex/',
  },
  {
    name: 'Color-Blind Safe Palettes',
    category: 'accessibility',
    description: 'Use color palettes distinguishable by users with protanopia, deuteranopia, and tritanopia (8% of men, 0.5% of women). Never rely on red/green distinction alone. Test with a color blindness simulator.',
    implementation: 'Use a vetted palette: blue, orange, teal, purple, pink (avoid pure red+green pairs). Tools: Viz Palette, ColorBrewer. In addition to color, use line patterns (solid, dashed, dotted) and point shapes (circle, square, triangle).',
    a11y: 'WCAG 1.4.1: Color is not the sole means of conveying information. Every color-coded element needs a secondary differentiator (shape, pattern, label, position). Test with Sim Daltonism or similar.',
    docsUrl: 'https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html',
  },
  {
    name: 'Chart Container ARIA',
    category: 'accessibility',
    description: 'The outermost chart wrapper needs role="img" with an aria-label summarizing what the chart shows. This is the first thing a screen reader announces — it should convey the chart type, subject, and key insight.',
    implementation: 'Wrap chart in <figure role="img" aria-label="Line chart showing monthly revenue from Jan to Dec 2025, trending upward 23%">. Add <figcaption> as a visible description.',
    a11y: 'aria-label should include: chart type, what is measured, time range, and the key insight or trend. Keep under 150 characters. Long descriptions go in aria-describedby linked to a <details> element.',
    docsUrl: 'https://www.w3.org/wiki/SVG_Accessibility/ARIA_roles_for_charts',
  },
  {
    name: 'Keyboard-Navigable Data Points',
    category: 'accessibility',
    description: 'Make individual data points focusable and navigable with arrow keys. On focus, show the tooltip with the point\'s value. Essential for users who cannot use a mouse to hover over chart elements.',
    implementation: 'Add tabIndex to data point SVG elements. Use arrow keys to move between points. Show tooltip on focus (same as hover). Recharts: use activeDot with custom focus handling.',
    a11y: 'Each data point: role="listitem" or focusable with aria-label including series name, category, and value. Arrow keys move sequentially through points. Escape exits chart navigation.',
    docsUrl: null,
  },
  {
    name: 'High Contrast Mode',
    category: 'accessibility',
    description: 'A toggle or automatic detection that increases chart contrast: thicker lines, larger labels, stronger fill colors, and pattern fills replacing solid colors. Essential for low-vision users.',
    implementation: 'Detect prefers-contrast: more in CSS. Apply: stroke-width increase (1.5→3), font-size increase, pattern fills via SVG <defs>. Use forced-colors media query for Windows High Contrast.',
    a11y: 'WCAG 1.4.11: Non-text contrast of 3:1 for chart elements. In high-contrast mode, increase to 4.5:1. Ensure axis labels and tick marks remain visible.',
    docsUrl: null,
  },
  {
    name: 'Sonification',
    category: 'accessibility',
    description: 'Represent data as sound — pitch maps to value, left-to-right panning maps to time. An experimental but powerful alternative for blind users to perceive trends, outliers, and patterns in charts without vision.',
    implementation: 'Use the Web Audio API or libraries like Highcharts Sonification module. Map y-values to pitch (low → high), x-axis to time. Play on button press. Include play/pause/speed controls.',
    a11y: 'Provide a "Listen to chart" button. Include controls for playback speed and volume. Announce start and end of sonification. This supplements — does not replace — the data table.',
    docsUrl: null,
  },

  // --- Real-Time & Streaming ---
  {
    name: 'Live-Updating Chart',
    category: 'real-time',
    description: 'A chart that receives new data points via WebSocket or SSE and appends them in real-time. Common for monitoring dashboards (server metrics, stock prices, IoT sensors). New points slide in from the right.',
    implementation: 'Use WebSocket or EventSource for data stream. Append to chart data array, remove oldest point to maintain a fixed window. Use requestAnimationFrame for smooth animation. Limit update frequency to 1-2 Hz for readability.',
    a11y: 'Do not announce every data update — it would overwhelm screen readers. Announce only threshold crossings: "CPU usage exceeded 90%". Provide a "Pause updates" button.',
    docsUrl: null,
  },
  {
    name: 'Threshold Alerts',
    category: 'real-time',
    description: 'Horizontal reference lines on charts indicating warning and critical thresholds. When values cross a threshold, visually highlight the violation with color change and optional animation. Essential for monitoring dashboards.',
    implementation: 'Recharts: ReferenceLine with label at y-threshold. Color the area above/below threshold with ReferenceArea. Change line/bar color when value exceeds threshold. Add pulse animation on breach.',
    a11y: 'Announce threshold crossings via aria-live="assertive": "Alert: Response time exceeded 500ms threshold". Threshold lines must be labeled with text, not just color.',
    docsUrl: null,
  },
  {
    name: 'Auto-Refresh with Indicator',
    category: 'real-time',
    description: 'Dashboard data refreshes on a timer (every 30s, 1m, 5m) with a visible countdown or "last updated" timestamp. Users must know when data is stale. Provide manual refresh and auto-refresh toggle.',
    implementation: 'Use setInterval or React Query refetchInterval. Show "Updated 30s ago" with a countdown ring or progress bar. Add refresh button (Lucide: RefreshCw icon). Store preference in localStorage.',
    a11y: 'Do not auto-refresh so frequently it disrupts screen reader users (minimum 30s). "Last updated" must be in an aria-live="polite" region. Provide a way to pause auto-refresh.',
    docsUrl: null,
  },
  {
    name: 'Streaming Data Buffer',
    category: 'real-time',
    description: 'Buffer incoming data points and render in batches to prevent jank from high-frequency updates. If data arrives at 100 Hz, batch into 1-2 Hz visual updates. Preserves all data for analysis while keeping the UI smooth.',
    implementation: 'Collect incoming events in an array. On each animation frame (or setInterval at 500ms), flush buffer to chart state. Use Web Worker for heavy aggregation. Show a "live" indicator dot that pulses.',
    a11y: 'No specific ARIA requirements — buffering is a performance optimization. Ensure the "live" indicator has aria-label="Receiving live data" for screen readers.',
    docsUrl: null,
  },
  {
    name: 'Time Window Selector',
    category: 'real-time',
    description: 'Quick-select buttons for common time ranges: "1h", "6h", "24h", "7d", "30d", "Custom". The primary navigation for time-series dashboards. Active range highlighted. Affects all charts on the dashboard.',
    implementation: 'shadcn: ToggleGroup with time range options. "Custom" opens DatePickerWithRange. Store in URL params for shareability. Highlight active option. Place in dashboard header for global scope.',
    a11y: 'role="radiogroup" with aria-label="Time range". Each option: role="radio" with aria-checked. Announce selection: "Showing last 24 hours".',
    docsUrl: null,
  },
  {
    name: 'Sparkline in Table Cell',
    category: 'real-time',
    description: 'Tiny inline charts inside data table cells showing the trend for each row\'s metric. Combines tabular precision with visual trend context. Common in financial dashboards, server monitoring, and analytics tables.',
    implementation: 'Recharts: minimal LineChart (no axes, no tooltip) sized to 80x24px inside a <td>. Color: green for uptrend, red for downtrend. Pair with the numeric value in an adjacent cell.',
    a11y: 'The sparkline is decorative — the adjacent cell contains the readable value. Add aria-hidden="true" on the sparkline. Use aria-label on the cell if the trend direction is meaningful: "Revenue: $4.2M, trending up".',
    docsUrl: null,
  },

  // --- Responsive & Mobile ---
  {
    name: 'Responsive Chart Container',
    category: 'responsive',
    description: 'Charts must resize fluidly with their container. Use a responsive wrapper that recalculates dimensions on resize. Never set fixed pixel widths on charts — they will overflow or leave whitespace on different screens.',
    implementation: 'Recharts: ResponsiveContainer (width="100%" height={300}). Tremor: responsive by default. For aspect-ratio preservation, use a CSS aspect-ratio container. Debounce resize events.',
    a11y: 'Ensure labels and legends remain readable at all sizes. If chart becomes too small, hide the chart and show the data table instead.',
    docsUrl: null,
  },
  {
    name: 'Axis Label Rotation',
    category: 'responsive',
    description: 'Rotate x-axis labels 45 or 90 degrees when they overlap at smaller widths. The fallback for long category labels in bar and line charts. Prefer horizontal labels when space permits — rotated text is harder to read.',
    implementation: 'Recharts: XAxis angle={-45} textAnchor="end". Auto-detect overlap and rotate only when needed. Alternative: truncate labels with ellipsis and show full label in tooltip.',
    a11y: 'Rotated text must remain readable at the final angle. Minimum font size 11px even when rotated. For screen readers, the full label text is in the DOM regardless of rotation.',
    docsUrl: null,
  },
  {
    name: 'Mobile Chart Simplification',
    category: 'responsive',
    description: 'On mobile, reduce chart complexity: fewer data points, simplified axes, larger touch targets, horizontal scroll for wide charts. Mobile users need trends, not precision — prioritize the story over the detail.',
    implementation: 'Downsample data for mobile (show weekly instead of daily). Hide secondary axes. Increase tick spacing. Show only the top 5 bars. Use a horizontal scrollable container for dense charts with snap points.',
    a11y: 'Touch targets for interactive points: minimum 44x44px. Tooltip triggered by tap (not hover). Ensure swipe does not conflict with page scrolling.',
    docsUrl: null,
  },
  {
    name: 'Chart Skeleton Loading',
    category: 'responsive',
    description: 'Show a placeholder skeleton matching the chart shape while data loads. Prevents layout shift and communicates that content is coming. Better than a spinner because it preserves the spatial context.',
    implementation: 'Render an SVG or div matching the chart dimensions with animated pulse backgrounds. shadcn: Skeleton component sized to match chart. Animate with CSS @keyframes. Transition to real chart with a fade.',
    a11y: 'aria-busy="true" on the chart container while loading. aria-label="Loading chart data". Remove aria-busy when data appears. Do not use aria-live for the loading state — only for the loaded state.',
    docsUrl: null,
  },
  {
    name: 'Stacked-to-Scrollable Layout',
    category: 'responsive',
    description: 'On desktop, show multiple charts side by side in a grid. On mobile, either stack vertically (for few charts) or use a horizontal scroll carousel (for many charts). Prevents tiny, unreadable charts on narrow screens.',
    implementation: 'CSS Grid with auto-fill, minmax(320px, 1fr). Below 320px per chart, switch to a single column. For dashboard tabs, use swipeable tab panels on mobile. Add scroll snap for horizontal carousels.',
    a11y: 'Horizontal scroll containers need role="region" with aria-label. Include visual scroll indicators. Ensure all charts are reachable via keyboard (Tab / arrow keys).',
    docsUrl: null,
  },
  {
    name: 'Print-Optimized Charts',
    category: 'responsive',
    description: 'Charts rendered for @media print: high contrast, no interactive elements, visible data labels, expanded legends, and appropriate sizing. Users print dashboards for meetings and reports.',
    implementation: 'Add @media print styles: force white background, increase stroke contrast, show all data labels, expand collapsed legends, remove hover-only elements. Use SVG for crisp printing at any DPI.',
    a11y: 'Print output should include all the information available interactively (annotations, tooltips as inline labels). Chart titles and legends must be visible in print.',
    docsUrl: null,
  },
];
