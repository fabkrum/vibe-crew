%% System Architecture Diagram
%% Generated by VibeCrew during Tier 1 foundation
%% Replace {{PLACEHOLDER}} values with TDR technology choices

flowchart TD
    subgraph Client["Client Layer"]
        Browser["Browser / Mobile"]
    end

    subgraph Edge["Edge / CDN"]
        CDN["{{CDN_PROVIDER}}"]
    end

    subgraph App["Application Layer"]
        Framework["{{FRAMEWORK}}"]
        API["API Routes"]
        Auth["{{AUTH_PROVIDER}}"]
    end

    subgraph Data["Data Layer"]
        DB["{{DATABASE}}"]
        ORM["{{ORM}}"]
        Cache["Cache (optional)"]
    end

    subgraph External["External Services"]
        Payments["{{PAYMENTS}} (if applicable)"]
        Email["{{EMAIL}} (if applicable)"]
        Storage["{{STORAGE}} (if applicable)"]
    end

    Browser --> CDN
    CDN --> Framework
    Framework --> API
    API --> Auth
    API --> ORM
    ORM --> DB
    API --> Cache
    API --> Payments
    API --> Email
    API --> Storage
