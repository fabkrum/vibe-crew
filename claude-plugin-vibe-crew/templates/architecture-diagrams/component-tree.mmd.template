%% Component Tree
%% Generated by VibeCrew during Tier 1 foundation (skeleton)
%% Extended by Builder during each Tier 2 feature
%% Arrow labels show data flow direction: props ↓, events ↑
%% Update when components are added, renamed, deleted, or re-parented

flowchart TD
    App["App"]
    Layout["Layout"]
    Header["Header"]
    Nav["Nav"]
    Main["Main / Outlet"]
    Footer["Footer"]

    App -->|"props ↓"| Layout
    Layout -->|"props ↓"| Header
    Layout -->|"props ↓"| Main
    Layout -->|"props ↓"| Footer
    Header -->|"props ↓"| Nav

    %% Builder: add new components below as they are created in Tier 2
    %% Place each component under its parent and show data flow direction
    %% Example:
    %% Main -->|"props ↓"| Dashboard
    %% Dashboard -->|"props ↓"| StatsCard
    %% Dashboard -->|"events ↑"| App
