%% Application State & User Flows
%% Generated by VibeCrew during Tier 1 foundation
%% Add primary user journeys from VISION.md personas
%% Update during Tier 2 when auth flows or state logic changes

stateDiagram-v2
    [*] --> Unauthenticated

    state Authentication {
        Unauthenticated --> LoginForm: visit protected route
        LoginForm --> Authenticating: submit credentials
        Authenticating --> Authenticated: success
        Authenticating --> LoginForm: failure
        Authenticated --> Unauthenticated: logout
    }

    state Authenticated {
        [*] --> Dashboard
        Dashboard --> FeatureA: navigate
        FeatureA --> Dashboard: back

        %% Add primary user flows below based on VISION.md
        %% Example:
        %% Dashboard --> CreateItem: click "New"
        %% CreateItem --> ItemDetail: save
        %% ItemDetail --> Dashboard: back
    }
